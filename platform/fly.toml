app = "carbonsim"
primary_region = "sin"

[build]

[env]
  OTREE_PRODUCTION = "1"

[http_service]
  internal_port = 8000
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1
  processes = ["app"]
