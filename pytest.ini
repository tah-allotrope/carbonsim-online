[pytest]
# Only the live game lives in engine/ and server/. The archived oTree platform
# under archive/ ships its own tests that error on collection (missing runtime
# dirs), so exclude it and pin the test roots — bare `pytest` then works.
testpaths = engine server
norecursedirs = archive .venv .git __pycache__ .pytest_cache
